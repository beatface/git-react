const React = require('react')
// const Home = require('../components/Home')

const Main = React.createClass({
    render: function() {
        return (
            <div>
                <h1>Hello!</h1>
            </div>
        )
    }
})

module.exports = Main
